@model Mst.Project.ViewModel.PaymentsViewModel

@{
}

<h2>Kayýt Detay</h2>

<div>
    <h4>Kayýt detaylarý</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PaymentDate)
        </dt>
        <dd>
            @(Model.PaymentDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Amount)
        </dt>
        <dd>
            @(Model.Amount)
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Düzenle", "Edit", new { customerNumber = Model.CustomerNumber, checkNumber = Model.CheckNumber }) |
    @Html.ActionLink("Sil", "Delete", new { customerNumber = Model.CustomerNumber, checkNumber = Model.CheckNumber }) |
    @Html.ActionLink("Listeye Dön", "Index")
</p>